# .env.example - Insight 基础设施的环境变量配置示例

# ==============================================================================
# ⚠️ 核心安全配置 (必须修改)
# ==============================================================================

# 数据库管理员密码 (Postgres)
DB_PASSWORD=change_me_please_2026

# 数据库基础配置
DB_HOST=insight-db
DB_PORT=5432
DB_USER=insight_admin

# ==============================================================================
# 🌐 网络与域名配置 (必须根据你的实际域名修改)
# ==============================================================================

# Docker 自定义网络名称
DOCKER_NETWORK_NAME=insight-net

# 各服务域名 (用于 n8n 及其它服务的内部链接跳转)
# 注意：这些域名需要在 Nginx Proxy Manager 中配置对应的 Proxy Host
N8N_HOST=flow.yourdomain.com
NOCODB_HOST=data.yourdomain.com
WIKIJS_HOST=wiki.yourdomain.com

# ==============================================================================
# 🔌 服务端口配置 (如无冲突建议保持默认)
# ==============================================================================

NGINX_PROXY_MANAGER_HTTP_PORT=80
NGINX_PROXY_MANAGER_HTTPS_PORT=443
NGINX_PROXY_MANAGER_ADMIN_PORT=81
N8N_PORT=5678
NOCODB_PORT=8080
WIKIJS_PORT=3000

# ==============================================================================
# 🌐 Docker 镜像加速器
# ==============================================================================

# 境内主机建议设为 true，境外部署建议设为 false
USE_DOCKER_MIRRORS=true
