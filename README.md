# 🚀 Project Team - Insight Infrastructure

这是 **Project Team** 的核心基础设施管理仓库。采用扁平化架构设计，实现“配置与数据分离”，支持一键部署与全量备份。

## 📂 目录结构

```text
.
├── manage.sh              # 唯一的管理入口 (init/up/down/backup/restore)
├── README.md              # 本项目操作说明
├── .gitignore             # Git 忽略配置（保护数据不外泄）
├── data/                  # [生产数据] 实际业务数据挂载点 (Git 忽略)
│   ├── 01-gateway/        # Nginx Proxy Manager 配置文件
│   └── 03-databases/      # Postgres 数据库原始文件
├── infra/                 # [核心配置] 
│   ├── compose/           # Docker Compose 蓝图文件 (.yml)
│   └── scripts/           # 自动化运维脚本 (backup/restore/setup)
└── exports/               # [备份产出] 存放生成的全量备份压缩包 (Git 忽略)
```

---

## 🛠️ 快速操作指南

### 1. 新服务器初始化 (开荒)
```bash
git clone https://github.com/akriamail/insight.git /opt/insight
cd /opt/insight
./manage.sh init    # 安装 Docker/Compose 环境
```

### 2. 日常管理
- **启动服务**: `./manage.sh up` (自动检查并初始化数据库)
- **停止服务**: `./manage.sh down`
- **查看状态**: `./manage.sh status`

### 3. 备份与灾备恢复
- **立即备份**: `./manage.sh backup`
- **全量恢复**: 将备份包放入 `exports/` 后运行 `./manage.sh restore`

---

## 🔐 维护规范
- **Git 提交**: 仅提交 `infra/` 下的配置变更，**严禁**手动修改 `data/` 结构。
- **数据安全**: `data/` 和 `exports/` 已被 `.gitignore` 忽略，请务必异地备份 `exports/` 中的包。

---
*Generated by Gemini for Project Team - 2026*
